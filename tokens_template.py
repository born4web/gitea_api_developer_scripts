"""
tokens_template.py

This file servees as a temaplete to show how tokens.py file shoul look like.

It defines only one variable TOKENS which contains the list of real Gitea authentication
tokens used by user for real Gitea implementations.

If not specified later in Gitea repository making script, we assume that default tokren[1] is selected

TOKENS = {
    1: "token1...",
    2: "token1...",
}
"""
TOKENS = {
    1: "",
}
